{% extends 'base.html.twig' %}
{% block body %}

<!-- Header Section End -->

    <div class="container text-center">
        <h1>Vente</h1>

         <table class="table mx-auto">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vente.id }}</td>
            </tr>
            <tr>
                <th>Quantitevendu</th>
                <td>{{ vente.quantitevendu }}</td>
            </tr>
            <tr>
                <th>Datevente</th>
                <td>{{ vente.datevente ? vente.datevente|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Montanttotal</th>
                <td>{{ vente.montanttotal }}</td>
            </tr>
        </tbody>
    </table>


      <a href="{{ path('app_vente_new') }}" class="btn btn-secondary"><i class="bi bi-arrow-left"></i></a>

<a href="{{ path('app_vente_edit', {'id': vente.id}) }}" class="btn btn-primary"><i class="bi bi-pencil"></i></a>
                     <a href="{{ path('app_vente_pdf', {'id': vente.id}) }}" class="btn btn-primary"><i class="fas fa-file-pdf"></i> PDF</a>
     <form action="{{ path('app_vente_delete', {'id': vente.id}) }}" method="post" style="display: inline-block;">

            <input type="hidden" name="_method" value="DELETE">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vente.id) }}">
            <button type="submit" class="btn btn-danger" onclick="return confirm('Êtes-vous sûr de vouloir supprimer cette vente ?')"><i class="bi bi-trash"></i></button>
        </form>
    </div>

{% endblock %}
