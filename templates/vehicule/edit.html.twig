{% extends 'base.html.twig' %}

{% block title %}Edit Vehicule{% endblock %}

{% block body %}
 <a href="{{ path('app_vehicule_index') }}" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i>
                </a>
    <div class="container mt-5">
        <h1 class="text-center mb-4">Edit Vehicule</h1>

        <div class="card">
            <div class="card-body">
                {{ include('vehicule/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
            <div class="card-footer text-right">
               
            </div>
        </div>

        <div class="card mt-4">
            <div class="card-header">
                <h3 class="mb-0">Delete Vehicule</h3>
            </div>
            <div class="card-body">
                {{ include('vehicule/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
